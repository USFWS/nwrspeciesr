% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/species_list.R
\name{download_species_list}
\alias{download_species_list}
\title{Download the NWRSpecies species list as a data frame}
\usage{
download_species_list(
  region_number = NULL,
  refuge_code = NULL,
  refuge_name = NULL,
  crosswalk = NULL,
  category_name = NULL,
  occurrence = NULL,
  itis_tsn = NULL,
  sort_by = NULL,
  rows_per_page = NULL,
  page_number = NULL,
  api_version = .nwrs_api_version
)
}
\arguments{
\item{region_number}{Integer or character scalar. USFWS legacy region
(1-8). Passed as \code{RegionNumber}.}

\item{refuge_code}{Character scalar. Cost center / unit code
(e.g. "FF08RANH00"). Passed as \code{RefugeCode}.}

\item{refuge_name}{Character scalar. Refuge name in any common form
(e.g. "Kenai", "kenai nwr", "Kenai National Wildlife Refuge"), resolved
to a \code{refuge_code} via the \link{refuges} crosswalk. Supply either
\code{refuge_code} or \code{refuge_name}, not both.}

\item{crosswalk}{Data frame with \code{refuge_code} and \code{refuge_name} columns
used to resolve \code{refuge_name}. If \code{NULL} (default), the bundled \link{refuges}
dataset is used.}

\item{category_name}{Character scalar. Taxonomic category (e.g. "Bird").
Validated against the documented category set; see
\code{\link[=get_species_categories]{get_species_categories()}} for the live list.}

\item{occurrence}{Character scalar. One of "NotPresent", "Present",
"ProbablyPresent", "Unconfirmed". See \code{\link[=get_species_occurrences]{get_species_occurrences()}}.}

\item{itis_tsn}{Integer or character scalar. ITIS Taxonomic Serial Number.}

\item{sort_by}{Character scalar. One of "OrdFamSci", "FamSci", "Sci".}

\item{rows_per_page}{Integer scalar. Rows per page (API default 100).}

\item{page_number}{Integer scalar. Page to return (API default 1).}

\item{api_version}{Character scalar. Required API version.}
}
\value{
A tibble parsed from the returned CSV.
}
\description{
Queries the CSV-only \code{SpeciesList/items/DownloadFile} endpoint. Combined
with a large \code{rows_per_page}, this is the documented way to retrieve all
records in a single call. Accepts the same filters as \code{\link[=get_species_list]{get_species_list()}}.
}
\examples{
\dontrun{
download_species_list(refuge_code = "FF08RANH00", rows_per_page = 130000)
}
}
