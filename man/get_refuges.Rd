% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_refuges.R
\name{get_refuges}
\alias{get_refuges}
\title{Get the refuge name / unit code crosswalk from the FWS Unit API}
\usage{
get_refuges()
}
\value{
A tibble with columns \code{refuge_code}, \code{refuge_name}, and
\code{region_number}.
}
\description{
Retrieves current national wildlife refuge units from the FWS Unit REST API
via the \pkg{fwsunitr} package and returns a crosswalk suitable for the
\code{crosswalk} argument of \code{\link[=get_species_list]{get_species_list()}} and \code{\link[=download_species_list]{download_species_list()}},
or as a live alternative to the bundled \link{refuges} dataset.
}
\details{
Requires the suggested \pkg{fwsunitr} package. The returned \code{refuge_code}
values are the unit (cost center) codes the NWRSpecies API expects as
\code{RefugeCode}. \code{region_number} is derived from the Unit API region code
(e.g. "R0007" becomes 7).
}
\examples{
\dontrun{
get_refuges()
get_species_list(refuge_name = "Kenai", crosswalk = get_refuges())
}
}
