% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{refuges}
\alias{refuges}
\title{USFWS refuge name / unit code crosswalk}
\format{
A tibble with one row per refuge and columns:
\describe{
\item{refuge_code}{Cost center / unit code (e.g. "FF07RYKD00"). Matches the
\code{RefugeCode} filter of the NWRSpecies API.}
\item{refuge_name}{Long refuge name (e.g. "Yukon Delta National Wildlife
Refuge").}
\item{region_number}{USFWS region number (1-8), derived from the Unit API
region code (e.g. "R0007" becomes 7). Note this reflects the Unit API's
current region, whereas the NWRSpecies API's own \code{RegionNumber} field is
documented as a legacy region; the two can differ for units affected by
regional reorganization. This column is informational only and is not
used when resolving \code{refuge_name}.}
}
}
\source{
FWS Unit REST API (\url{https://iris.fws.gov/APPS/Unit}), retrieved
via the \pkg{fwsunitr} package. See \code{data-raw/refuges.R}.
}
\usage{
refuges
}
\description{
A one-to-one crosswalk between refuge unit (cost center) codes and refuge
names, used by \code{\link[=get_species_list]{get_species_list()}} and \code{\link[=download_species_list]{download_species_list()}} to resolve
a \code{refuge_name} argument to the \code{RefugeCode} the NWRSpecies API expects.
}
\details{
This is a bundled snapshot. For a live table, use \code{\link[=get_refuges]{get_refuges()}}, which
pulls current refuge units from the FWS Unit REST API via the \pkg{fwsunitr}
package. Regenerate the snapshot with \code{data-raw/refuges.R}.
}
\keyword{datasets}
